# Default ProGuard/R8 rules. Minification is disabled for this challenge, so no custom keep
# rules are required. kotlinx.serialization model classes are kept automatically by the
# Kotlin serialization R8 rules shipped with the library, and Room generates its own.
